import {
  bubblesort,
  selectionsort,
  mergesort,
  quicksort,
  countingsort,
  radixsort,
} from "/sorts/index";
import SORT_ALGORITHM from "/public/sort_algorithms";

function getSortingAnimation(array, algorithm) {
  console.log(SORT_ALGORITHM);
}

export { getSortingAnimation };
